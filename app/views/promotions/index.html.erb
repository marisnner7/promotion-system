<%= "Nenhuma promoção cadastrada" if @promotions.empty? %>

<%= render 'search', promotion: @promotion %>

<%= render 'card', promotion: @promotion %>

<%= link_to 'Voltar' , root_path %>


<%= link_to 'Registrar uma promoção', new_promotion_path %>